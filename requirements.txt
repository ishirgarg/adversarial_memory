# ── Core LLM APIs ────────────────────────────────────────────────────────────
openai>=1.30.0
anthropic>=0.79.0

# ── Memory systems ───────────────────────────────────────────────────────────
mem0ai>=1.0.3

# ── SimpleMem dependencies ───────────────────────────────────────────────────
# Vector store
lancedb>=0.8.0
pyarrow>=14.0.0
tantivy                         # Full-text search index used by LanceDB

# Embeddings
sentence-transformers>=2.7.0
numpy>=1.26.0

# Retrieval
rank-bm25>=0.2.2
litellm>=1.0.0

# Date/time parsing used by the hybrid retriever
dateparser>=1.2.0

# ── LOCOMO benchmark metrics ─────────────────────────────────────────────────
nltk>=3.8.0                     # BLEU, METEOR
rouge-score>=0.1.2              # ROUGE
bert-score>=0.3.13              # BERTScore

# ── Chat UI ──────────────────────────────────────────────────────────────────
streamlit>=1.37.0

# ── General utilities ────────────────────────────────────────────────────────
tiktoken>=0.7.0                 # Token counting
requests>=2.32.0
python-dotenv>=1.0.0
tqdm>=4.66.0
pandas>=2.0.0

# ── Optional: download the LoCoMo dataset via --download flag ────────────────
# datasets>=2.20.0
